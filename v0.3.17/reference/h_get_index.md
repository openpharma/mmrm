# Test if the First Vector is Subset of the Second Vector

Test if the First Vector is Subset of the Second Vector

## Usage

``` r
h_get_index(x, y)
```

## Arguments

- x:

  (`vector`) the first list.

- y:

  (`vector`) the second list.
