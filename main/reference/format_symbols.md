# Format Symbol Objects

For printing, variable names are converted to symbols and deparsed to
use R's built-in formatting of variables that may contain spaces or
quote characters.

## Usage

``` r
format_symbols(x)
```

## Arguments

- x:

  (`character`) A vector of variable names.

## Value

A formatted string of comma-separated variables.
